<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>contributor Dashboard</title>
    <link rel="stylesheet" href="/stylesheets/studashboard.css">
    <link rel="stylesheet" href="/stylesheets/contrischedule.css">
</head>
<body>
    <div class="main-details">
        <div class="details">
            <p>hii  <%=data%></p>
           <a href="">logout</a>
        </div>
       </div>
   <div class="dashboard-menu">
       <div class="menu">
       <ul>
           <li><a href="/contridash?username=<%=data%>">Profile</a></li>
           <li><a href="/contridash/schedule?username=<%=data%>">Schedule a class</a></li>
           <li><a href="">Classes Scheduled</a></li>
           <li><a href="/contridash/contriclassesdone?username=<%=data%>&status=done">Classes Done</a></li>
       </ul>
    </div>
   </div>
   <div class="main-container">
       <div class="form-details">
        <table>
            <tr>
              <th>Classtopic</th>
              <th>subject</th>
              <th>status</th>
            </tr>
            <%if(contridata){%>
            <%contridata.forEach((data)=>{%>
                <%if(data.status===null){%>
                <tr>
                <td><%=data.classTopic%></td>
                 <td><%=data.subject%></td>
                 <td><form action="/scheduled/submit/<%=data._id%>?username=<%=data.username1%>" method="POST"><select name="status" id="status">
                    <option value="done">done</option>
                    <option value="cancel">cancel</option>
                 </select>
                 <input type="submit">
                </form></td>
                </tr>
                <%}%>
                <%})%>
           
            <%}%>
        </table>
  </div>
   </div>
</body>
</html>